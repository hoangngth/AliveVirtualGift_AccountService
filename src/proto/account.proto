syntax = "proto3";

package proto;

message Status {
    bool success = 1;
}

message LoginRequest {
    string username = 1;
    string password = 2;
}

message LogoutRequest {
    string token = 1;
}

message CreateRequest {
    string username = 1;
    string password = 2;
    string name = 3;
    string email = 4;
}

message UpdateRequest {
    string name = 1;
    string email = 2;
    string avatar = 3;
    string password = 4;
}

message ShowRequest {
    uint64 id = 1;
}

// Define Account Service
service AccountService {
    rpc Login(LoginRequest) returns (Status);
    rpc Logout(LogoutRequest) returns (Status);
    rpc Create(CreateRequest) returns (Status);
    rpc Update(UpdateRequest) returns (Status);
    rpc Show(ShowRequest) returns (Status);
}
